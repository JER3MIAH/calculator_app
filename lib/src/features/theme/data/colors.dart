import 'package:flutter/material.dart';

var appColors = AppColors();

class AppColors {
  // Theme 1
  final theme1Background =
      const HSLColor.fromAHSL(1.0, 222, 0.26, 0.31).toColor();
  final theme1KeypadBG =
      const HSLColor.fromAHSL(1.0, 223, 0.31, 0.20).toColor();
  final theme1DisplayBG =
      const HSLColor.fromAHSL(1.0, 224, 0.36, 0.15).toColor();

  final theme1DelKeyBg =
      const HSLColor.fromAHSL(1.0, 225, 0.21, 0.49).toColor();
  final theme1DelKeyHover =
      const HSLColor.fromAHSL(1.0, 224, 0.51, 0.76).toColor();

  final theme1EqualsKeyBG =
      const HSLColor.fromAHSL(1.0, 6, 0.63, 0.50).toColor();
  final theme1EqualsKeyHover =
      const HSLColor.fromAHSL(1.0, 6, 0.93, 0.67).toColor();

  final theme1KeyBG = const HSLColor.fromAHSL(1.0, 30, 0.25, 0.89).toColor();
  final theme1KeyHover = const HSLColor.fromAHSL(1.0, 31, 1.0, 1.0).toColor();

  final theme1ButtonText =
      const HSLColor.fromAHSL(1.0, 221, 0.14, 0.31).toColor();
  final whiteText = const HSLColor.fromAHSL(1.0, 0, 0.0, 1.0).toColor();

  //* Theme 2
  final theme2Background = const HSLColor.fromAHSL(1.0, 0, 0.0, 0.90).toColor();
  final theme2KeypadBG = const HSLColor.fromAHSL(1.0, 0, 0.05, 0.81).toColor();
  final theme2DisplayBG = const HSLColor.fromAHSL(1.0, 0, 0.0, 0.93).toColor();

  final theme2DelKeyBg =
      const HSLColor.fromAHSL(1.0, 185, 0.42, 0.37).toColor();
  final theme2DelKeyHover =
      const HSLColor.fromAHSL(1.0, 184, 0.40, 0.56).toColor();

  final theme2EqualsKeyBG =
      const HSLColor.fromAHSL(1.0, 25, 0.98, 0.40).toColor();
  final theme2EqualsKeyHover =
      const HSLColor.fromAHSL(1.0, 25, 1, 0.61).toColor();

  final theme2KeyBG = const HSLColor.fromAHSL(1.0, 45, 0.07, 0.89).toColor();
  final theme2KeyHover = const HSLColor.fromAHSL(1.0, 0, 0.0, 1).toColor();

  final theme2ButtonText =
      const HSLColor.fromAHSL(1.0, 60, 0.10, 0.19).toColor();

  //* Theme 3
  final theme3Background =
      const HSLColor.fromAHSL(1.0, 268, 0.75, 0.09).toColor();
  final theme3DisplayBackground =
      const HSLColor.fromAHSL(1.0, 268, 0.71, 0.12).toColor();

  final theme3DelKeyBg =
      const HSLColor.fromAHSL(1.0, 281, 0.89, 0.26).toColor();
  final theme3DelKeyHover =
      const HSLColor.fromAHSL(1.0, 280, 0.56, 0.44).toColor();

  final theme3EqualsKeyBG =
      const HSLColor.fromAHSL(1.0, 176, 1.00, 0.44).toColor();
  final theme3EqualsKeyShadow =
      const HSLColor.fromAHSL(1.0, 290, 0.70, 0.36).toColor();
  final theme3EqualsKeyHover =
      const HSLColor.fromAHSL(1.0, 176, 1, 0.79).toColor();

  final theme3KeyBG = const HSLColor.fromAHSL(1.0, 268, 0.47, 0.21).toColor();
  final theme3KeyHover =
      const HSLColor.fromAHSL(1.0, 268, 0.54, 0.44).toColor();

  final theme3ButtonText =
      const HSLColor.fromAHSL(1.0, 52, 1.00, 0.62).toColor();
  // final theme3ButtonText =
  //     const HSLColor.fromAHSL(1.0, 198, 0.20, 0.13).toColor();
}
