import 'package:flutter/material.dart';

var appColors = AppColors();

class AppColors {
  // Theme 1
  final theme1Background =
      const HSLColor.fromAHSL(1.0, 222, 0.26, 0.31).toColor();
  final theme1KeypadBackground =
      const HSLColor.fromAHSL(1.0, 223, 0.31, 0.20).toColor();
  final theme1DisplayBackground =
      const HSLColor.fromAHSL(1.0, 224, 0.36, 0.15).toColor();

  final desaturatedDarkBlueKeyBackground =
      const HSLColor.fromAHSL(1.0, 225, 0.21, 0.49).toColor();
  final desaturatedDarkBlueKeyShadow =
      const HSLColor.fromAHSL(1.0, 224, 0.28, 0.35).toColor();

  final redKeyBackground =
      const HSLColor.fromAHSL(1.0, 6, 0.63, 0.50).toColor();
  final darkRedKeyShadow =
      const HSLColor.fromAHSL(1.0, 6, 0.70, 0.34).toColor();

  final lightGrayishOrangeKeyBackground =
      const HSLColor.fromAHSL(1.0, 30, 0.25, 0.89).toColor();
  final grayishOrangeKeyShadow =
      const HSLColor.fromAHSL(1.0, 28, 0.16, 0.65).toColor();

  final veryDarkGrayishBlueText =
      const HSLColor.fromAHSL(1.0, 221, 0.14, 0.31).toColor();
  final whiteText = const HSLColor.fromAHSL(1.0, 0, 0.0, 1.0).toColor();

  // Theme 2
  final theme2Background = const HSLColor.fromAHSL(1.0, 0, 0.0, 0.90).toColor();
  final theme2KeypadBackground =
      const HSLColor.fromAHSL(1.0, 0, 0.05, 0.81).toColor();
  final theme2DisplayBackground =
      const HSLColor.fromAHSL(1.0, 0, 0.0, 0.93).toColor();

  final darkModerateCyanKeyBackground =
      const HSLColor.fromAHSL(1.0, 185, 0.42, 0.37).toColor();
  final veryDarkCyanKeyShadow =
      const HSLColor.fromAHSL(1.0, 185, 0.58, 0.25).toColor();

  final orangeKeyBackground =
      const HSLColor.fromAHSL(1.0, 25, 0.98, 0.40).toColor();
  final darkOrangeKeyShadow =
      const HSLColor.fromAHSL(1.0, 25, 0.99, 0.27).toColor();

  final lightGrayishYellowKeyBackground =
      const HSLColor.fromAHSL(1.0, 45, 0.07, 0.89).toColor();
  final darkGrayishOrangeKeyShadow =
      const HSLColor.fromAHSL(1.0, 35, 0.11, 0.61).toColor();

  final veryDarkGrayishYellowText =
      const HSLColor.fromAHSL(1.0, 60, 0.10, 0.19).toColor();
  final whiteTextTheme2 = const HSLColor.fromAHSL(1.0, 0, 0.0, 1.0).toColor();

  // Theme 3
  final theme3Background =
      const HSLColor.fromAHSL(1.0, 268, 0.75, 0.09).toColor();
  final theme3DisplayBackground =
      const HSLColor.fromAHSL(1.0, 268, 0.71, 0.12).toColor();

  final darkVioletKeyBackground =
      const HSLColor.fromAHSL(1.0, 281, 0.89, 0.26).toColor();
  final vividMagentaKeyShadow =
      const HSLColor.fromAHSL(1.0, 285, 0.91, 0.52).toColor();

  final pureCyanKeyBackground =
      const HSLColor.fromAHSL(1.0, 176, 1.00, 0.44).toColor();
  final softCyanKeyShadow =
      const HSLColor.fromAHSL(1.0, 177, 0.92, 0.70).toColor();

  final veryDarkVioletKeyBackground =
      const HSLColor.fromAHSL(1.0, 268, 0.47, 0.21).toColor();
  final darkMagentaKeyShadow =
      const HSLColor.fromAHSL(1.0, 290, 0.70, 0.36).toColor();

  final lightYellowText =
      const HSLColor.fromAHSL(1.0, 52, 1.00, 0.62).toColor();
  final veryDarkBlueText =
      const HSLColor.fromAHSL(1.0, 198, 0.20, 0.13).toColor();
  final whiteTextTheme3 = const HSLColor.fromAHSL(1.0, 0, 0.0, 1.0).toColor();
}
